services:
  - type: web
    name: church-management-system
    env: node
    region: oregon
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      sizeGB: 1
    autoDeploy: true
    healthCheckPath: /
