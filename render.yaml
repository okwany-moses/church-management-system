services:
  - type: web
    name: church-management-system
    env: docker
    region: oregon
    plan: starter
    dockerfilePath: Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      sizeGB: 1
    autoDeploy: true
    healthCheckPath: /
